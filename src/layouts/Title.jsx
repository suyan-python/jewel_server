import React from "react";

const Title = (props) => {
  return (
    <div>
      <p className="font-medium" style={{ color: `${props.color}` }}>
        Jewel Himalayan Products
      </p>
    </div>
  );
};

export default Title;
